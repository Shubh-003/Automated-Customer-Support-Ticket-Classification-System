DB_USER = Your_Db_Name
DB_PASSWORD = Your_DB_Password

GEMINI_API_KEY = Your_ApiKey

JWT_SECRET = Your_Secret
JWT_EXPIRATION: e.g.86400000

{
  "username": "admin",
  "password": "admin123"
}
==== HOW TO RUN =====
cd D:\smart-support-desk\frontend
npm run dev
http://localhost:5174/










=============================
## How will USER vs ADMIN/AGENT UI be different?
We will read the role from JWT login response.
Your backend already returns role inside the token (or user info).
Example login response:
{
  "accessToken": "...",
  "refreshToken": "...",
  "role": "ADMIN"
}

We store role in:
AuthContext

Example Auth State
{
  user: "shubh",
  role: "ADMIN",
  token: "..."
}


Phase 2: Authentication UI (Login + Register)
axiosClient.js:
 const axiosClient = axios.create({
               baseURL: "http://localhost:8080/api",
             })
store token at localstorage:  const token = localStorage.getItem("accessToken");

-> Now every request automatically attaches the JWT.

RegisterPage: usestte: usename and password then handleSubmit for axios.post("/auth/register")

CORS configuration in securityConfig.java- CorsConfigurationSource bean
🔐 Important Production Note:
Later in production we usually allow: https://yourdomain.com  not *.
Example: config.setAllowedOrigins(List.of("https://supportdesk.com"));


Phase 5 — Role-Based Dashboard (User / Agent / Admin):
Install JWT Decoder : We need to read the role from the token.

Phase 6: status update:
PUT /api/tickets/{id}/status
{
  "status": "IN_PROGRESS"
}

phase 7: WebSockets for real-time ticket updates
Backend publishes WebSocket event -> All connected clients receive event-> React updates UI instantly

Total Tickets
Open Tickets
Resolved Tickets
Active Agents
Recent Tickets
====================================
// First add path here for new functionality then add route in app.jsx
// then add lable in AdminLayout


@Query("""
SELECT t FROM Ticket t
WHERE LOWER(t.subject) LIKE LOWER(CONCAT('%', :query, '%'))
   OR LOWER(t.description) LIKE LOWER(CONCAT('%', :query, '%'))
""")
List<Ticket> searchTickets(@Param("query") String query); ** add id sesrch slso


src
 ├─ layouts
 │   ├─ UserLayout.jsx
 │   └─ AdminLayout.jsx
 │
 ├─ pages
 │   ├─ user
 │   │   ├─ UserDashboard.jsx
 │   │   ├─ CreateTicket.jsx
 │   │   └─ MyTickets.jsx
 │   │
 │   └─ admin
 │       ├─ AdminDashboard.jsx
 │       ├─ AdminTickets.jsx
 │       ├─ Users.jsx
 │       └─ Categories.jsx
 │
 ├─ components
 │   └─ Sidebar.jsx

** Reusable layout for admin and user
1.Sidebar Component(UI) -> Admin/User Layout(Dash links) -> Pages Actual logics -> App.jsx imports and routes.

 Spring Boot REST API
 JWT authentication
 RBAC security
 AI ticket classification
 PostgreSQL database
 React frontend
 Protected routes
 Role-based UI
 Ticket workflow
 Auto-close scheduler
 WebSocket real-time updates
 Notification bell counter
 Username in navbar
 Sidebar dashboard layout